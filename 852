class Solution {
public:
    int peakIndexInMountainArray(vector<int>& arr) {
            int start = 0;

            while(arr[start] < arr[start + 1])
            {
                start++;
            }   

            return start;
    }
};
